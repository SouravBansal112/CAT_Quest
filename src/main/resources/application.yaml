spring:
  application:
    name: CatQuest

  datasource:
    url: jdbc:postgresql://localhost:5432/catquest_db
    username: ren
    password: "#1Invincible"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    debug: true
jwt:
  secret: "my-very-long-32-plus-character-secret-key-here-and-it-is-gonna-work"
  access-expiration-ms: 900000        # 15 minutes
  refresh-expiration-ms: 604800000    # 7 days